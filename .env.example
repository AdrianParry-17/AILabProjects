# Frontend (copy values into frontend/.env.local if needed)
VITE_API_BASE_URL=http://localhost:8000/api/v1
VITE_ENABLE_OSM_TILES=true

# Backend (optional; safe defaults are built in)
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173,http://localhost:4173
ROUTING_DATASET_PATH=backend/data/da_nang_osm_snapshot.json
