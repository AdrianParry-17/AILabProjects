"""Iterative-Deepening A* (IDA*) — placeholder.

Owned by the IDA* teammate. Implement `ida_star()` (and a registered `SearchAlgorithm`
subclass, matching `algorithms/bfs.py`) per ALGORITHM_SPEC.md. Until then calling
`ida_star()` raises `NotImplementedError` so consumers fail loudly instead of silently
getting an unfinished implementation.
"""

from __future__ import annotations

from shared.types import GraphLike


def ida_star(
    graph: GraphLike,
    start: str,
    goal: str,
    enable_logging: bool = True,
):
    """IDA* — not implemented yet (owner: IDA* teammate)."""
    raise NotImplementedError(
        "IDA* is not implemented yet; it is owned by the IDA* teammate "
        "(algorithms/ida_star.py)."
    )
